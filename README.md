# OSS Loop Tracker

Status tracking for the OSS Contribution Loop. This repo is Cursor-free — 
status updates and work packets live here. Code fixes go directly to forks → upstream PRs.

Cursor only gets involved when a patch file is pushed to the `cursor-review/` 
directory on `Idaluna-Labs/idaluna-opportunity-monitor`.
